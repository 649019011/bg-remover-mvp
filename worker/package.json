{
  "name": "bg-remover-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AI Background Remover",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put REMOVEBG_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
